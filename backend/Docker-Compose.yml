version: '3'

services:
  api-stock:
    container_name: api-stock
    build: .
    platform: linux/amd64 
    environment:
      - SECRET={SECRET}
      - DATABASE_URL={DATABASE_URL}
    ports:
      - "3333:3333"